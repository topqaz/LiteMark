# ===========================================
# LiteMark 后端配置示例
# 复制此文件为 .env 并修改配置
# ===========================================

# -------------------------------------------
# 基础配置
# -------------------------------------------
# 应用名称
APP_NAME=LiteMark API

# 调试模式 (生产环境设为 false)
DEBUG=false

# -------------------------------------------
# 数据库配置
# -------------------------------------------
# SQLite (默认)
DATABASE_URL=sqlite+aiosqlite:///./litemark.db

# PostgreSQL (可选)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/litemark

# MySQL (可选)
# DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/litemark

# -------------------------------------------
# JWT 认证配置
# -------------------------------------------
# JWT 密钥 (⚠️ 生产环境必须修改为随机字符串)
JWT_SECRET=your-super-secure-random-key-change-this

# JWT 算法
JWT_ALGORITHM=HS256

# Token 过期天数
JWT_EXPIRE_DAYS=7

# -------------------------------------------
# CORS 跨域配置
# -------------------------------------------
# 允许的跨域来源 (* 表示允许所有)
# 多个来源用逗号分隔: http://localhost:3000,https://example.com
CORS_ORIGINS=*


# -------------------------------------------
# 默认管理员账号
# 仅在首次启动时创建，之后可在后台修改
# -------------------------------------------
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
